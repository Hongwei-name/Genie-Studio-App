# 智元标注审核助手 - 开发文档

> **Genie Studio 平台 · Tampermonkey 油猴脚本**
>
> 一个在 Agibot 旗下 Genie Studio 数采标注平台上运行的油猴（Tampermonkey）增强脚本，自动监控待审核 EP、精准定位验收失败待返工 EP，并提供审核统计功能。
>
> **脚本作者**：陈洪伟
> **脚本名称（统一）**：智元标注审核助手
> **内部代号**：EP 质检员
> **目标平台**：Genie Studio（`https://tgs-geniestudio.agibot.com`，Agibot 旗下）

---

## 目录

- [一、项目背景与需求](#一项目背景与需求)
  - [1.1 业务痛点](#11-业务痛点)
  - [1.2 核心需求（必须实现）](#12-核心需求必须实现)
  - [1.3 边缘需求（辅助功能）](#13-边缘需求辅助功能)
- [二、业务流程与数据模型](#二业务流程与数据模型)
  - [2.1 EP 阶段流转图](#21-ep-阶段流转图)
  - [2.2 stage_workflow 数据结构](#22-stage_workflow-数据结构)
  - [2.3 EP 状态字段](#23-ep-状态字段)
- [三、API 接口规范](#三api-接口规范)
  - [3.1 基础信息](#31-基础信息)
  - [3.2 任务列表接口](#32-任务列表接口)
  - [3.3 Job 列表接口](#33-job-列表接口)
  - [3.4 EP 列表接口](#34-ep-列表接口)
- [四、核心筛选逻辑详解](#四核心筛选逻辑详解)
  - [4.1 待审核 EP 筛选（服务端过滤）](#41-待审核-ep-筛选服务端过滤)
  - [4.2 验收失败待返工 EP 筛选（客户端分析）](#42-验收失败待返工-ep-筛选客户端分析)
  - [4.3 初筛人筛选（可选）](#43-初筛人筛选可选)
  - [4.4 两套筛选逻辑对比](#44-两套筛选逻辑对比)
- [五、数据获取与性能优化方案](#五数据获取与性能优化方案)
  - [5.1 分页处理规范](#51-分页处理规范)
  - [5.2 并发请求池](#52-并发请求池)
  - [5.3 验收失败 EP 获取完整流程](#53-验收失败-ep-获取完整流程)
- [六、功能特性与操作说明](#六功能特性与操作说明)
  - [6.1 三 Tab 布局说明](#61-三-tab-布局说明)
  - [6.2 配置项](#62-配置项)
  - [6.3 EP 跳转 URL](#63-ep-跳转-url)
- [七、系统架构与代码结构](#七系统架构与代码结构)
  - [7.1 模块划分](#71-模块划分)
  - [7.2 面板生命周期管理](#72-面板生命周期管理)
- [八、Bug 发现与修复汇总](#八bug-发现与修复汇总)
  - [Bug-001：待审核 EP 包含已审核数据](#bug-001待审核-ep-包含已审核数据)
  - [Bug-002：验收失败 EP 严重搜索不全](#bug-002验收失败-ep-严重搜索不全)
  - [Bug-003：CSS 语法错误导致脚本报错](#bug-003css-语法错误导致脚本报错)
  - [Bug-004：面板多开 / 双开（4 轮迭代最终修复）](#bug-004面板多开--双开4-轮迭代最终修复)
- [九、修复方法论总结](#九修复方法论总结)
- [十、完整场景示例](#十完整场景示例)
  - [场景 A：验收失败待返工（应显示）](#场景-a验收失败待返工应显示)
  - [场景 B：已返工重新提交（不显示）](#场景-b已返工重新提交不显示)
  - [场景 C：直接验收通过（不显示）](#场景-c直接验收通过不显示)

---

## 一、项目背景与需求

### 1.1 业务痛点

Genie Studio 是一个数据采集与标注平台。采集员采集的数据经过 5 个阶段流转：

```
采集(collect) → 上传(upload) → 后处理(post_process) → 初筛(screening) → 验收(acceptance)
```

每个阶段由不同角色处理。初筛员（如作者陈洪伟等）负责初筛阶段，验收员（如验收岗同事等）负责验收阶段。

**两大痛点**：

1. **待审核 EP 无法统一查看**：平台没有一个聚合的待审核列表，初筛员需要逐个任务点开才能看到需要审核的 EP。
2. **验收失败返工 EP 无入口**：如果验收阶段被拒绝，EP 会退回初筛阶段重做。但平台没有"验收失败待返工"列表，初筛员不知道哪些 EP 被打回来了，只能靠手动逐个任务查找。

### 1.2 核心需求（必须实现）

| 编号 | 需求 | 说明 |
|------|------|------|
| 1 | 待审核 EP 监控 | 自动获取待审核任务数量，以及待审核 EP 的直达链接，点击一键跳转到审核页面 |
| 2 | 已审核数量统计 | 实时统计当日已完成审核的 EP 数量 |
| 3 | 已审核视频时长统计 | 根据已标注 EP 的帧数换算视频时长，并持久化到第二天重置 |
| 4 | 验收失败返工 EP 列表 | 获取"初筛之后验收失败"的 EP，方便返工。待审核 EP 和验收失败 EP 分离显示，验收失败 EP 不实时刷新，提供按钮手动触发获取 |

### 1.3 边缘需求（辅助功能）

| 编号 | 需求 | 说明 |
|------|------|------|
| 5 | 简洁面板 UI | 采用玻璃拟态风格，三 Tab 布局，信息密度高，操作简单 |
| 6 | 配置页面 | 支持配置自动刷新频率、重置统计数据、清空日志、自动新标签页打开 EP 链接等 |
| 7 | 按初筛人筛选验收失败 EP | 在配置页可填写初筛人用户名，精准定位属于自己的返工 EP |
| 8 | 代码结构规范 | 模块化分层（状态管理 / API 请求 / UI 渲染 / 事件绑定），便于维护和迭代 |

---

## 二、业务流程与数据模型

### 2.1 EP 阶段流转图

```
          ┌───────────┐   ┌───────────┐   ┌──────────────┐   ┌──────────────┐   ┌──────────────┐
          │ collect   │──▶│  upload   │──▶│ post_process │──▶│  screening   │──▶│ acceptance   │
          │ 采集      │   │ 上传      │   │ 后处理       │   │ 初筛         │   │ 验收         │
          └───────────┘   └───────────┘   └──────────────┘   └──────┬───────┘   └──────┬───────┘
                                                                     │ 验收失败           │
                                                                     ▼ 退回初筛           ▼ 验收通过
                                                              等待返工处理            流程结束
```

**正常流转各阶段状态序列**：
```
collect(success) → upload(success) → post_process(success)
  → screening(pending → running → success)
  → acceptance(pending → running → success)
```

**验收失败返工流转**：
```
... → screening(success) → acceptance(pending → running → failed, reject)
  → screening(pending)  ← 退回初筛，等待返工
    → 如果返工完成：screening(success) → acceptance(pending)
```

### 2.2 stage_workflow 数据结构

每个 EP 对象有一个 `stage_workflow` 数组，**按时间顺序**记录了该 EP 从采集到当前状态的完整流转历史。这是所有筛选逻辑的**核心依据**。

```json
{
  "stage_workflow": [
    {
      "stage_code": "collect",
      "stage_name": "采集",
      "stage_status": "success",
      "action": "transition",
      "operator": "system",
      "operator_display_name": "system",
      "reason": null,
      "created_at": "2026-07-29 16:49:37"
    },
    {
      "stage_code": "screening",
      "stage_name": "初筛",
      "stage_status": "success",
      "action": "review",
      "operator": "chenhongwei",
      "operator_display_name": "陈洪伟",
      "reason": null,
      "created_at": "2026-07-29 19:06:35"
    },
    {
      "stage_code": "acceptance",
      "stage_name": "验收",
      "stage_status": "failed",
      "action": "reject",
      "operator": "ys-acceptance_a",
      "operator_display_name": "验收-验收员A",
      "reason": "审核标签统一改成通过—有色差",
      "created_at": "2026-07-28 16:37:52"
    },
    {
      "stage_code": "screening",
      "stage_name": "初筛",
      "stage_status": "pending",
      "action": "transition",
      "operator": "system",
      "operator_display_name": "system",
      "reason": "验收失败退回初筛",
      "created_at": "2026-07-28 16:37:53"
    }
  ]
}
```

**每条记录字段详解**：

| 字段 | 类型 | 说明 | 典型值 |
|------|------|------|--------|
| `stage_code` | string | 阶段代码（英文标识） | `collect` / `upload` / `post_process` / `screening` / `acceptance` |
| `stage_name` | string | 阶段中文名（展示用） | 采集 / 上传 / 后处理 / 初筛 / 验收 |
| `stage_status` | string | 阶段状态 | `pending`(待处理) / `running`(进行中) / `success`(完成) / `failed`(失败) |
| `action` | string | 操作类型 | `create`(创建) / `transition`(流转) / `review`(审核) / `reject`(拒绝) |
| `operator` | string | 操作人用户名（用于筛选匹配） | `chenhongwei` / `screener_b` / `ys-acceptance_a` / `system` |
| `operator_display_name` | string | 操作人显示名（展示用） | `陈洪伟` / `初筛员B` / `验收-验收员A` / `system` |
| `reason` | string | 操作原因（验收拒绝时为失败原因） | `审核标签统一改成通过—有色差` / 验收失败退回初筛` / `null` |
| `created_at` | string | 操作时间 | `2026-07-28 16:37:52` |

### 2.3 EP 状态字段

| 字段值 | 含义 | 说明 |
|--------|------|------|
| `9` | 待审核 | 整数状态码，可用于 API 参数过滤 |
| `check_pass` | 验收通过 | 字符串状态 |
| `check_reject` | 验收拒绝 | 字符串状态 |
| `2` | 验收失败（旧） | 旧版整数状态码，**不可靠**，判断必须以 `stage_workflow` 为准 |

> ⚠️ **重要警告**：`status` 整数字段为平台历史遗留字段，部分 EP 的 `status` 可能不准确。判断「验收失败待返工」必须**优先使用 `stage_workflow` 链路分析**，不能只看 `status` 字段。

---

## 三、API 接口规范

### 3.1 基础信息

| 项目 | 值 |
|------|-----|
| **Base URL** | `https://tgs-geniestudio.agibot.com/data/api/v1` |
| **认证方式** | 浏览器 Cookie（通过 Tampermonkey 的 `GM_xmlhttpRequest` 自动携带当前登录态） |
| **响应格式** | JSON |
| **统一响应结构** | `{ code: number, message: string, data: any }` |
| **成功标识** | `code === 0` |

### 3.2 任务列表接口

获取当前用户下所有数采任务的分页列表。

```
GET /collect/tasks
```

**请求参数**：

| 参数 | 类型 | 必填 | 示例 | 说明 |
|------|------|------|------|------|
| `page_num` | int | 是 | `1` | 页码，从 1 开始 |
| `page_size` | int | 是 | `20` | 每页数量，建议 20 |

**响应结构（data）**：
```json
{
  "total": 27,
  "page_num": 1,
  "page_size": 20,
  "list": [
    {
      "id": 6836,
      "name": "南宁数采-顾客遗弃在篮内的水果归位",
      "type": "GENIE",
      "status": 2,
      "creator_name": "任务创建人",
      "not_check_count": 15,
      "description": "任务描述文本..."
    }
  ]
}
```

**任务对象关键字段**：
| 字段 | 说明 |
|------|------|
| `id` | 任务 ID，后续 Job / EP 接口的必需参数 |
| `name` | 任务名称（展示用） |
| `not_check_count` | 未审核 EP 数量（展示用） |

### 3.3 Job 列表接口

获取某个任务下所有 Job（子批次）的分页列表。

```
GET /collect/task/job?task_id={taskId}
```

**请求参数**：

| 参数 | 类型 | 必填 | 示例 | 说明 |
|------|------|------|------|------|
| `task_id` | int | 是 | `6923` | 任务 ID（从上一接口取） |
| `page_num` | int | 是 | `1` | 页码 |
| `page_size` | int | 是 | `10` | 每页数量，建议 10 |

**响应结构（data.list 中的单个 Job）**：
```json
{
  "id": 1366863,
  "task_id": 6923,
  "name": "job-name",
  "status": 3,
  "variables": [
    {
      "id": 5494791,
      "task_id": 6923,
      "job_id": 1366863,
      "name": "Q5XPxXoEodEXgZRZnEQhj",
      "display_name": "果蔬",
      "values": "[13011]"
    }
  ],
  "not_check_count": 0
}
```

**⚠️ 提取 job_id 必须穷举两种路径**：

1. **`job.id`** — Job 对象本身的 ID（**主要，始终存在**）
2. **`job.variables[].job_id`** — 变量列表中冗余的 job_id（可选，部分 Job 的 variables 为空数组）

提取时：同时遍历两种方式，并用 **Set 去重**，确保不遗漏任何一个 job_id。

### 3.4 EP 列表接口

获取某个 Job 下所有 EP（Episode，单个数据条目）的分页列表。这是核心接口，分为两种调用方式。

```
GET /collect/tasks/{taskId}/jobs/{jobId}/episodes
```

**请求参数**：

| 参数 | 类型 | 必填 | 示例 | 说明 |
|------|------|------|------|------|
| `taskId` | path | 是 | `6642` | 任务 ID（URL 路径） |
| `jobId` | path | 是 | `1364798` | Job ID（URL 路径） |
| `status[]` | int[] | 否 | `9` | **待审核过滤专用**：值为 9 时只返回待审核 EP |
| `with_status_count` | bool | 否 | `true` | 是否返回状态计数统计，建议 true |
| `page_num` | int | 是 | `1` | 页码 |
| `page_size` | int | 是 | `10` | 每页数量，建议 10 |

**✅ 获取待审核 EP（服务端过滤，性能好）**：
```
GET /collect/tasks/6642/jobs/1364798/episodes?status[]=9&with_status_count=true&page_num=1&page_size=10
```
URL 编码后：`status%5B%5D=9`

**✅ 获取全部 EP（验收失败筛选用，客户端过滤）**：
```
GET /collect/tasks/6642/jobs/1364798/episodes?with_status_count=true&page_num=1&page_size=10
```
注意：**不要加 `status[]` 参数**，这样才会返回所有状态的 EP，方便后续在客户端判断验收失败情况。

**响应中的单个 EP 对象关键字段**：

```json
{
  "id": 1370847,
  "uuid": "c98e346f-a60a-54f2-807b-f6e48d80395a",
  "task_id": 6642,
  "job_id": 1364798,
  "status": "check_reject",
  "sn_code": "G2A0004BC00857",
  "text": {
    "description": "1",
    "extra": ["{黄色}"]
  },
  "stage_workflow": [ /* 详见第二节 */ ],
  "current_stage": {
    "stage_code": "acceptance",
    "stage_name": "验收",
    "stage_status": "success",
    "role": 99
  }
}
```

---

## 四、核心筛选逻辑详解

### 4.1 待审核 EP 筛选（服务端过滤）

**筛选方式**：通过 API 参数在服务端过滤，不需要客户端分析。

**请求 URL 必须包含**：`status[]=9`（URL 编码后 `status%5B%5D=9`）

**完整流程**：

```
1. 获取当前页面的 taskId / jobId（或遍历所有任务）
2. 请求 EP 列表接口，参数中加上 status[]=9
3. 服务端只返回状态为「待审核」(status=9) 的 EP
4. 前端直接渲染这些 EP 为可点击按钮
```

**特点**：
- ✅ 性能高（服务端过滤，返回数据量小）
- ✅ 支持自动刷新
- ❌ 无法找到验收失败后卡在初筛的 EP（因为那些 EP 的 API 层面 status 可能不是 9）

---

### 4.2 验收失败待返工 EP 筛选（客户端分析）

**筛选方式**：遍历所有任务 → 所有 Job → 所有 EP，然后在客户端通过分析 `stage_workflow` 链路精准判断。

**⚠️ 核心判断算法**（共 3 步）：

#### 步骤 1：找到「最后一次验收拒绝」记录

从 `stage_workflow` **末尾向前**遍历，找到第一条满足以下 3 个条件的记录：
- `stage_code === 'acceptance'`
- `stage_status === 'failed'`
- `action === 'reject'`

**找不到 → 直接排除**（该 EP 从未被验收拒绝过，不属于验收失败）。

#### 步骤 2：检查「拒绝之后是否已经返工」

在步骤 1 找到的拒绝记录**之后**（数组更大的索引）的所有记录中，检查是否存在以下任一情况：

| 条件 | 含义 | 判定 |
|------|------|------|
| `stage_code === 'screening'` 且 `stage_status === 'success'` | 已重新初筛通过 | ❌ 已返工，**排除** |
| `stage_code === 'screening'` 且 `action === 'review'` | 已有人操作过初筛审核 | ❌ 已返工，**排除** |
| `stage_code === 'acceptance'` 且 `action === 'transition'` | 已重新提交到验收阶段 | ❌ 已返工，**排除** |

**以上全部不存在 → 判定为「验收失败待返工」，应显示 ✅**

#### 步骤 3：（可选）按初筛人过滤

如果配置了初筛人筛选，继续检查：

在「最后一次验收拒绝」记录**之前**（数组更小的索引）的记录中，查找是否存在：
- `stage_code === 'screening'` 且 `stage_status === 'success'`
- 且 `operator` 或 `operator_display_name` 与配置的初筛人匹配（不区分大小写，支持精确和包含匹配）

找到 → 属于该初筛人 ✅；找不到 → 排除 ❌。

#### 伪代码实现

```javascript
function isEpFailed(ep) {
    if (!ep.stage_workflow || ep.stage_workflow.length === 0) {
        return ep.status === 2;  // 兜底：无 stage_workflow 时用旧字段
    }
    var stages = ep.stage_workflow;

    // 步骤 1：从后往前找最后一次验收拒绝
    var lastRejectIdx = -1;
    for (var i = stages.length - 1; i >= 0; i--) {
        var s = stages[i];
        if (s.stage_code === 'acceptance' &&
            s.stage_status === 'failed' &&
            s.action === 'reject') {
            lastRejectIdx = i;
            break;
        }
    }
    if (lastRejectIdx === -1) return false;  // 从未被验收拒绝

    // 步骤 2：检查拒绝之后是否已返工
    for (var i = lastRejectIdx + 1; i < stages.length; i++) {
        var s = stages[i];
        if (s.stage_code === 'screening') {
            if (s.stage_status === 'success' || s.action === 'review') {
                return false;  // 已重新初筛
            }
        }
        if (s.stage_code === 'acceptance' && s.action === 'transition') {
            return false;  // 已重新提交验收
        }
    }

    return true;  // 验收失败，且未返工
}
```

#### 四种典型场景判定

| 场景 | stage_workflow 最后几条 | 判定结果 |
|------|------------------------|---------|
| **验收失败待返工** | `acceptance(failed, reject)` → `screening(pending)` | ✅ 显示 |
| **刚被拒绝（尚未流转）** | `... → acceptance(failed, reject)`（无后续记录） | ✅ 显示 |
| **已返工并重新验收中** | `acceptance(failed, reject)` → `screening(success)` → `acceptance(pending)` | ❌ 不显示（已返工） |
| **从未被拒绝，直接通过** | `... → acceptance(success)` | ❌ 不显示（无拒绝） |

---

### 4.3 初筛人筛选（可选）

在配置页填写初筛人用户名（如 `chenhongwei`）后，验收失败列表只会显示「此人初筛过、且被验收拒绝后未返工」的 EP。

**匹配规则**：
1. 定位到「最后一次验收拒绝」的索引位置
2. 在该位置**之前**遍历（只看这次拒绝之前的初筛记录）
3. 查找 `stage_code === 'screening' && stage_status === 'success'` 的记录
4. 比较 operator 字段：
   - `operator`（用户名）精确匹配 / 包含匹配
   - `operator_display_name`（显示名）精确匹配 / 包含匹配
   - 全部**不区分大小写**
5. 任一匹配成功 → 属于该初筛人 ✅

伪代码：
```javascript
function isEpByScreener(ep, screener) {
    if (!screener) return true;  // 未配置则显示全部
    // ... 定位 lastRejectIdx
    var screenerLower = screener.toLowerCase();
    for (var i = lastRejectIdx - 1; i >= 0; i--) {
        var s = stages[i];
        if (s.stage_code === 'screening' && s.stage_status === 'success') {
            var op = (s.operator || '').toLowerCase();
            var opName = (s.operator_display_name || '').toLowerCase();
            if (op === screenerLower || opName === screenerLower ||
                op.indexOf(screenerLower) !== -1 || opName.indexOf(screenerLower) !== -1) {
                return true;
            }
        }
    }
    return false;
}
```

### 4.4 两套筛选逻辑对比

| 维度 | 待审核 EP | 验收失败待返工 EP |
|------|----------|-----------------|
| **过滤位置** | 服务端（API 参数） | 客户端（代码分析 stage_workflow） |
| **筛选条件** | `status[]=9` | 3 步算法：找最后一次拒绝 → 检查之后是否返工 →（可选）初筛人匹配 |
| **数据范围** | 当前任务 / Job 下待审核 EP | **所有任务 × 所有 Job × 所有 EP** |
| **刷新方式** | 自动刷新（可配置频率） | 手动点击「获取验收失败EP」按钮触发（不自动刷新） |
| **性能** | 高（服务端过滤，数据量小） | 中（5 路并发遍历，约 6 秒获取 27 任务 × 100+ Job） |

---

## 五、数据获取与性能优化方案

### 5.1 分页处理规范

所有 `list` 类型接口**都必须处理分页**，绝对不能假设只有一页。

**标准循环分页模板**：
```javascript
async function fetchAllPaginated(buildUrlFn, pageSize) {
    var allItems = [];
    var pageNum = 1;
    while (true) {
        var pageData = await buildUrlFn(pageNum, pageSize);
        allItems = allItems.concat(pageData);
        if (pageData.length < pageSize) {
            break;  // 返回数量小于 pageSize，说明是最后一页
        }
        pageNum++;
    }
    return allItems;
}
```

**停止条件**：当某页返回的 `list.length < page_size` 时，说明已经没有下一页了。

**不这样做的后果**：每个 Job 可能有几十上百个 EP，只取第 1 页会严重漏掉数据（如实际 23 条只显示 1 条）。

---

### 5.2 并发请求池

由于需要遍历「所有任务 × 所有 Job × 所有 EP」，串行请求速度非常慢（约 30 秒），必须使用并发请求。

**通用并发请求池实现**：

```javascript
/**
 * 并发执行一组异步任务，维持最多 limit 个活跃请求
 * @param {Array} items - 要处理的元素数组
 * @param {Function} fn - (item, idx) => Promise，处理单个元素的异步函数
 * @param {number} limit - 最大并发数（建议 5）
 * @returns {Promise<Array>} 按原顺序返回每个任务的结果
 */
function runConcurrent(items, fn, limit) {
    var results = new Array(items.length);
    var index = 0;
    var active = 0;
    var completed = 0;
    var total = items.length;

    return new Promise(function(resolve) {
        if (total === 0) { resolve(results); return; }

        function scheduleNext() {
            // 只要还有空位、还有任务，就持续调度
            while (active < limit && index < total) {
                var currentIdx = index;
                var item = items[currentIdx];
                index++;
                active++;

                fn(item, currentIdx)
                    .then(function(result) { results[currentIdx] = result; })
                    .catch(function()     { results[currentIdx] = null; })
                    .then(function() {
                        active--;
                        completed++;
                        if (completed >= total) {
                            resolve(results);
                        } else {
                            scheduleNext();  // 一个完成，立即调度下一个
                        }
                    });
            }
        }

        scheduleNext();  // 启动
    });
}
```

**并发度建议**：`limit = 5`（既不会被平台限流，也能显著提速）。

**性能对比（实测 27 任务 / 100+ Job 场景）**：

| 方式 | 总耗时 |
|------|-------|
| 串行请求 | ~30 秒 |
| 5 路并发 | ~6 秒 |

---

### 5.3 验收失败 EP 获取完整流程

```
┌──────────────────────────────────────────────────────────────────────┐
│              验收失败待返工 EP 获取完整流程                             │
├──────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  Phase 1：获取所有任务                                                │
│  ┌─────────────────────────────────────────────────────┐             │
│  │ fetchAllTasksForFailedEp()                           │             │
│  │   ├─ 分页调用 GET /collect/tasks                     │             │
│  │   ├─ page_size=20，循环到返回不足一页                 │             │
│  │   └─ 输出：[{id, name, status, ...}]                │             │
│  └─────────────────────────────────────────────────────┘             │
│                         ↓ N 个任务                                    │
│  Phase 2：并发获取每个任务的 Job 列表（5 路并发）                       │
│  ┌─────────────────────────────────────────────────────┐             │
│  │ runConcurrent(taskIds, fetchAllJobsForTask, 5)       │             │
│  │   每个：fetchAllJobsForTask(taskId)                  │             │
│  │     ├─ 分页 GET /collect/task/job?task_id=xxx        │             │
│  │     └─ 返回该任务下所有 Job                           │             │
│  │                                                      │             │
│  │  提取 job_id（双路径 + Set 去重）：                    │             │
│  │    seenIds.add(job.id)                               │             │
│  │    job.variables.forEach(v → seenIds.add(v.job_id)) │             │
│  │   ↓                                                  │             │
│  └─ 输出：[{ taskId, jobId }, ...] × M 个去重后 Job       │             │
│                         ↓ M 个 Job                                      │
│  Phase 3：并发获取每个 Job 的 EP 列表（5 路并发）                         │
│  ┌─────────────────────────────────────────────────────┐             │
│  │ runConcurrent(jobSpecs, fetchAllEpsForJob, 5)        │             │
│  │   每个：fetchAllEpsForJob(taskId, jobId)              │             │
│  │     ├─ 分页 GET .../episodes（不加 status[]=9）       │             │
│  │     └─ 返回该 Job 下所有状态的 EP                     │             │
│  └─ 输出：所有 EP 扁平数组                                │             │
│                         ↓ 所有 EP                                       │
│  Phase 4：客户端筛选                                                     │
│  ┌─────────────────────────────────────────────────────┐             │
│  │ eps.filter(ep →                                     │             │
│  │     isEpFailed(ep)             // 是否验收失败待返工  │             │
│  │     && isEpByScreener(ep, screener) // 初筛人过滤    │             │
│  │ )                                                    │             │
│  └─ 输出：最终展示的 EP 列表                              │             │
│                                                                      │
│  Phase 5：渲染结果 + 统计信息                                           │
│  └─ 显示：共扫描 N 任务 / M Job / X EP，发现 Y 条待返工                  │
│                                                                      │
└──────────────────────────────────────────────────────────────────────┘
```

---

## 六、功能特性与操作说明

### 6.1 三 Tab 布局说明

```
┌─────────────────────────────────────────────────┐
│ [Agibot任务监控]  [17待审] [完成0] [0s]  [—][✕] │ ← 标题栏：标题 + 徽章 + 最小化关闭
├─────────────────────────────────────────────────┤
│ [●待审核EP]  [ 验收失败EP① ]  [ 日志/配置 ]     │ ← Tab 切换，① 为数量徽章
├─────────────────────────────────────────────────┤
│                                                 │
│ （内容区域，根据选中 Tab 变化）                     │
│                                                 │
└─────────────────────────────────────────────────┘
```

| Tab | 功能 | 刷新方式 |
|-----|------|---------|
| **待审核 EP** | 展示当前/所有任务的待审核 EP 按钮，红色=验收失败过的 | 自动刷新（可配置频率） |
| **验收失败 EP** | 展示所有「验收失败待返工」的 EP，带失败原因 | 手动点击「获取验收失败EP」按钮 |
| **日志/配置** | 上半部分：操作日志面板<br>下半部分：配置表单 | - |

**视觉风格**：玻璃拟态（Liquid Glass）
- `backdrop-filter: blur(24px) saturate(1.4)`
- 半透明背景 `rgba(30, 36, 49, 0.78)`
- 内边框高光 + 阴影，现代感强

### 6.2 配置项

在「日志/配置」Tab 下半部分的配置表单中设置，保存后写入本地存储。

| 配置项 | 存储键 | 默认值 | 说明 |
|--------|--------|--------|------|
| 自动刷新频率 | `agibotRefreshInterval` | `60000` (60秒) | 待审核 EP 自动刷新间隔，毫秒。设为 0 则暂停自动刷新 |
| 自动新标签页打开 | `agibotAutoOpen` | `true` | 点击 EP 按钮时是否自动在新标签页打开审核页面 |
| 初筛人筛选 | `agibotScreener` | `""` (空) | 验收失败 EP 的初筛人过滤，填写用户名（如 `chenhongwei`），留空显示全部 |
| 当日审核数量 | `agibotTodayStats` | `{}` | 自动持久化，次日自动重置。<br>`{ "YYYY-MM-DD": { count: N, frames: N } }` |
| 暂停状态 | `agibotPaused` | `false` | 是否暂停自动刷新 |

**可调常量（代码中 CONFIG 对象）**：
```javascript
const CONFIG = {
    CONCURRENCY_LIMIT: 5,       // 并发请求池最大并发数
    TASK_PAGE_SIZE: 20,         // 任务列表每页数量
    JOB_PAGE_SIZE: 10,          // Job 列表每页数量
    EP_PAGE_SIZE: 10,           // EP 列表每页数量
    DEBOUNCE_MS: 300,           // 路由切换防抖毫秒数
    INIT_LEFT: 16,              // 面板初始左偏移
    INIT_TOP: 88                // 面板初始上偏移
};
```

### 6.3 EP 跳转 URL

点击任何 EP 按钮，打开对应审核页面（如果开启了自动打开，则自动在新标签打开）：

```
https://tgs-geniestudio.agibot.com/data/collection/tasks/{taskId}/jobs/{jobId}/episodes/{epId}/check
```

三个 ID 都来自 EP 对象本身。

---

## 七、系统架构与代码结构

### 7.1 模块划分

（单文件脚本，通过注释划分模块，见 `trea_v2.js`）

| 模块 | 功能说明 | 核心对象/函数 |
|------|---------|--------------|
| **常量层** | 所有 ID、存储键、配置常量 | `PANEL_ID` / `SCRIPT_KEY` / 各 `STORAGE_KEY_*` / `CONFIG` |
| **实例守卫** | 防止脚本被注入运行多次 | `window[SCRIPT_KEY]` 存在性检查 + `INSTANCE.instanceId` |
| **Store（本地存储）** | 封装所有 `GM_getValue / GM_setValue` 操作 | `Store.getStats / setStats / getAutoOpen / getScreener ...` |
| **State（状态管理）** | 运行时状态（面板状态、暂停、当前 EP 列表、拖拽等） | `State.autoOpen / isPaused / failedEps / currentTaskEps / drag ...` |
| **日志（Log）** | 日志追加渲染和滚动 | `Log.add(type, msg)` |
| **统计（Stats）** | 审核数量/时长累加与渲染 | `Stats.add() / render()` |
| **PAGE 判断** | 当前 URL 类型识别 + URL 参数提取 | `PAGE.isTaskList() / isTaskDetail() / isCheck() / getTaskId() / getJobId()` |
| **API 层** | 封装所有 `GM_xmlhttpRequest` 调用 + 业务筛选 | `API.fetchTasks / fetchEps / fetchFailedEpsFromAllTasks / isEpFailed / isEpByScreener` |
| **UI 渲染** | 面板构建、DOM 元素、徽章更新 | `buildPanel() / buildResourcePanel() / renderEps() / renderFailedEps() / addStyles()` |
| **拖拽模块** | 面板移动、最小化 | `startDrag() / endDrag() / toggleMini() / toggleResourceMini()` |
| **事件绑定** | Tab 切换、按钮点击、下拉、配置保存 | 在 `buildPanel()` 末尾统一绑定 |
| **生命周期** | 启动 / 停止 / 清理 / 路由切换 | `start() / fullCleanup() / onRouteChange() / initTaskListPage() / startCheckMonitor()` |

### 7.2 面板生命周期管理（防双开核心）

面板多开是这个项目最棘手的 Bug（迭代了 4 轮才根治），最终规范的生命周期如下：

#### 启动的「单一入口 + 令牌链」模型

整个系统只有 `start()` 一个入口，任何路由变化 / 初始化 最终都走 start()。

```
           初始化 DOMContentLoaded
                    │
                    ▼
           ┌─ start(token=undefined) ─┐
           │                           │
┌─────────▼──────────┐   ┌──────────▼────────────┐
│  onRouteChange()   │   │ 1. phase 检查：         │
│  每次 pushState    │   │    if starting/running  │
│  popstate hash     │   │       → 直接 return     │
│  change 触发       │   │ 2. startToken++         │
│  都走这里          │   │ 3. phase='starting' ←原子上锁│
│                    │   │ 4. fullCleanup() 清旧面板 │
│  routeToken++      │   │ 5. initTaskListPage(    │
│  setTimeout(300ms) │   │      myStartToken)      │
│  仅最后一次生效    │   │    ├─ safeRemovePanel() │
│                    │   │    ├─ await waitForBody│
└─────────┬──────────┘   │    ├─ Token 校验（过期→放弃）│
          │              │    ├─ buildPanel()    │
          └──────────────┘    │   ├─ 同步双重清理 │
                              │   └─ 创建并挂载   │
                              │ 6. phase='running'│
                              └────────────────────┘
```

#### 令牌与锁的四层防护

| 防护层 | 位置 | 防止的问题 |
|--------|------|-----------|
| 第 1 层 `routeToken` | `onRouteChange` | 防抖期间多次触发路由，只有最后一次生效 |
| 第 2 层 `phase` 原子检查 | `start()` 入口 | 两次 start 并发，后一次被拒绝 |
| 第 3 层 `startToken` | `start()` → `initTaskListPage` 内 await 后 | 异步等待期间路由变更，过期的 start 自动放弃 |
| 第 4 层 同步双重清理 | `buildPanel()` 开头的瞬间 | 极端竞态下（两个 buildPanel 先后执行），后执行的那个会先把前一个刚创建的 DOM 立即删掉，再创建自己 |

#### 关键约束（必须严格遵守）

1. **`fullCleanup()` 绝不修改 `INSTANCE.phase`**。只清理资源（DOM / 定时器 / Observer），生命周期状态统一由 `start()` 管理，由用户手动关闭时显式设置 `phase='idle'`。
2. **异步 `await` 之后必须做 Token 检查**。因为 await 让出了事件循环，路由可能在等待期间又变了。
3. **`buildPanel()` 必须在创建 DOM 的**同一同步代码块内**先做清理**。不能先在 `initTaskListPage` 里 safeRemovePanel，再 await，再 build——中间的异步空隙就是竞态窗口。

---

## 八、Bug 发现与修复汇总

本节记录开发过程中所有 Bug 的完整排查与修复历史，供他人参考避坑。

---

### Bug-001：待审核 EP 包含已审核数据

**现象**：「待审核EP」Tab 展示了大量已经审核通过/拒绝的 EP，无法区分真正需要处理的。

**根因**：`API.fetchEps()` 调用 EP 列表接口时 URL 缺少筛选参数 `status[]=9`。后端在无此参数时返回**所有状态**的 EP。

```
❌ 错误：GET .../episodes?with_status_count=true&page_num=1
✅ 正确：GET .../episodes?status%5B%5D=9&with_status_count=true&page_num=1
```

**修复**：在 URL 构造中加上 `status%5B%5D=9`（即 `status[]=9` URL 编码后）。

**影响**：修复前混杂 100+ 条，修复后只显示 17 条真正待审核的。

---

### Bug-002：验收失败 EP 严重搜索不全

**现象**：用户实际有 23+ 条验收失败待返工 EP，但面板只显示 1 条。

这个 Bug 由 **4 个独立问题叠加**导致，必须全部修复才有效：

#### 子问题 A：未处理分页

每个 Job 可能有 25+ 个 EP，但代码只请求了 `page_num=1`，自然只剩前 10 条。

**修复**：将 `fetchAllJobsForTask` / `fetchAllEpsForJob` 改为循环分页，`list.length < page_size` 时停止。

#### 子问题 B：job_id 提取不完整

Job 有两处可提取 id：`job.id` 和 `job.variables[].job_id`。旧代码只从 variables 提取，但部分 Job 的 variables 为空数组，导致这些 Job 的所有 EP 完全没被扫描到。

**修复**：双路径提取 + Set 去重：
```javascript
seenIds.add(job.id);
if (job.variables) job.variables.forEach(v => seenIds.add(v.job_id));
```

#### 子问题 C：验收失败判断逻辑不精准

旧版 `isEpFailed` 只检查 `stage_workflow` 最后一条是否 `screening+pending`。结果：
- 刚被验收拒绝尚未流转（最后是 acceptance+failed）→ **漏掉**
- 已返工并重新提交验收（最后是 acceptance+pending）→ **误显示**

**修复**：改为「最后一次拒绝追踪法」——找最后一次拒绝记录，再检查其后是否已返工（详见 4.2 节）。

#### 子问题 D：初筛人筛选范围错误

旧代码在整个 stage_workflow 中搜索初筛人，导致匹配到返工之后的初筛记录。正确做法应该只搜索「最后一次验收拒绝之前」的初筛记录。

**修复**：定位 `lastRejectIdx` 后，`for (i = lastRejectIdx - 1; i >= 0; i--)`。

---

### Bug-003：CSS 语法错误导致脚本报错

**现象**：脚本加载时报错 `TS1137: 应为表达式或逗号`，整个脚本无法运行。

**根因**：编辑长 CSS 数组时，把 `GM_addStyle([...])` 的闭合 `]);` 误写成了字符串闭合 `'");'`。

**修复**：改回 `]);`，并确保每行 CSS 字符串正确用单引号包裹。

---

### Bug-004：面板多开 / 双开（4 轮迭代最终修复）

**现象**：页面同时出现 2 个或多个「Agibot任务监控」面板，各自独立运行。

这个 Bug 前后迭代了 4 个版本才根治，每一次的假设和新发现如下：

#### 修复 v1：防抖 + 导航锁（失败）

方案：`onRouteChange` 触发后，设置 `navigationLock = true`，300ms 后 `fullCleanup + start`，再解锁。

**失败原因**：React Router 会在解锁后立即再次触发 pushState（如 SPA 内部路由连续跳转），锁已经释放，第二次 start 顺利执行。

**时间线复现**：
```
T0: onRouteChange → 锁=true, setTimeout(300ms)
T1: 300ms到 → fullCleanup, start(创建面板A), 锁=false
T2: React Router 又触发一次 pushState → 锁=false，不拦截 → start(创建面板B)
结果：两个面板！
```

#### 修复 v2：令牌 routeToken 替代锁（部分成功）

方案：每次 onRouteChange 递增 `routeToken`，setTimeout 回调时对比 `myToken === routeToken`，只有最后一次会执行。

**新问题**：onRouteChange 收敛为单次了，但 start() 内部还有并发竞态（见 v3）。

#### 修复 v3：发现 fullCleanup 对 phase 的污染

发现致命问题：
```javascript
async function start() {
    if (phase === 'starting') return;       // 入口检查
    INSTANCE.phase = 'starting';            // 先设 starting
    fullCleanup();                          // ❌ 但 fullCleanup() 内部写了 phase='idle'！
    // 这里 phase 被覆盖回 idle，并发的 start() 就能通过检查
    initTaskListPage();
}
```

同时 `initTaskListPage` 又有：
```javascript
async function initTaskListPage() {
    if (phase === 'starting') return;  // ❌ start() 已经设 starting，这里直接 return，面板创建不出来
    phase = 'starting';
    // ...
}
```

**修复**：
1. `fullCleanup()` 删除 `phase = 'idle'`，不再碰 phase
2. `initTaskListPage` 删除入口 phase 检查（start() 已设好状态），职责分离

#### 修复 v4：startToken + buildPanel 同步双重清理

最终增加的两道防线：
1. **await 后 startToken 检查**：异步等待期间路由变更，过期的 startToken 直接 return
2. **buildPanel 中的同步清理**：在同一同步块先 safeRemovePanel + getElementById 兜底，再创建新的 DOM。即便是极端竞态下两个 buildPanel 交替执行，后执行的那个也会先把前一个刚创建的 DOM 立即删掉。

**最终状态**：连续快速切换路由 20 次，始终只出现 1 个面板。

---

## 九、修复方法论总结

从本次 3 大类 7 个子 Bug 的修复过程中，总结出以下可复用的原则：

| # | 原则 | 对应 Bug | 说明 |
|---|------|---------|------|
| 1 | **所有 list 接口必须分页** | Bug-002A | 绝不要假设返回不足一页。没有 `while(true)` + `length < page_size` 停止逻辑，就等于搜索不全。 |
| 2 | **ID 提取穷举所有可能路径** | Bug-002B | 不要只看文档里"应该"的字段。平台有冗余字段时，要全部考虑并用 Set 去重。 |
| 3 | **链路判断优于快照判断** | Bug-002C | 工作流不能只看"最后一条记录是什么状态"，要找到**关键锚点**（这里是最后一次拒绝）再检查前后。 |
| 4 | **关系型匹配要带时间窗口** | Bug-002D | "这个初筛是那次验收前的还是返工后的？"时间/位置顺序是核心，不能只看有没有某个字段。 |
| 5 | **长字符串数组编辑用小步修改** | Bug-003 | 编辑几百行的 CSS 字符串数组时，一次只改一小段并立即检查语法，不要大面积替换。 |
| 6 | **锁（Lock）不如令牌（Token）可靠** | Bug-004 v1→v2 | 带"窗口期"的锁（锁→操作→解锁），解锁后立即触发同样事件会绕过。令牌每次递增、过期的自动失效，不存在窗口期。 |
| 7 | **状态由上层生命周期函数统一管理** | Bug-004 v3 | 资源清理函数（fullCleanup）不要修改 phase / 生命周期状态。这类全局状态只应由最外层的 start() / stop() 统一管理，下层函数越权修改 = 污染。 |
| 8 | **await 让出的时间必须做过期校验** | Bug-004 v4 | JavaScript 单线程不等于没有并发。任何 `await` 之后，外部状态可能完全变了——必须用 Token 检查自己还是不是最新的。 |
| 9 | **DOM 创建前做同步的兜底清理** | Bug-004 v4 | 哪怕 1ms 前刚 safeRemovePanel，在 appendChild 前的**同一个同步块**里再做一次 `getElementById → remove` 兜底。竞态不怕多查。 |

---

## 十、完整场景示例

以下三个真实场景的完整 `stage_workflow`，帮助开发者快速验证自己的 `isEpFailed` 实现是否正确。

### 场景 A：验收失败待返工（应显示 ✅）

```json
{
  "id": 1370847,
  "stage_workflow": [
    { "stage_code": "collect",      "stage_status": "success", "action": "transition" },
    { "stage_code": "upload",       "stage_status": "success", "action": "transition" },
    { "stage_code": "post_process", "stage_status": "success", "action": "transition" },
    { "stage_code": "screening",    "stage_status": "success", "action": "review",
      "operator": "chenhongwei", "operator_display_name": "陈洪伟" },
    { "stage_code": "acceptance",   "stage_status": "pending", "action": "transition" },
    { "stage_code": "acceptance",   "stage_status": "failed",  "action": "reject",
      "operator": "ys-acceptance_a", "reason": "审核标签统一改成通过—有色差" },
    { "stage_code": "screening",    "stage_status": "pending", "action": "transition",
      "reason": "验收失败退回初筛" }
  ]
}
```

**判定步骤**：
1. 从后往前找，第 5 条是最后一次 `acceptance + failed + reject` → lastRejectIdx=5 ✅
2. 检查 index=6 之后：只有 `screening + pending`（退回待处理），没有 `screening.success` 或 `acceptance.transition` → **未返工** ✅
3. 结论：属于验收失败待返工 → **显示**

---

### 场景 B：已返工重新提交（不显示 ❌）

```json
{
  "id": 1371087,
  "stage_workflow": [
    { "stage_code": "collect",      "stage_status": "success" },
    { "stage_code": "screening",    "stage_status": "success", "action": "review",
      "operator": "chenhongwei", "operator_display_name": "陈洪伟" },
    { "stage_code": "acceptance",   "stage_status": "failed",  "action": "reject",
      "reason": "标签错误" },
    { "stage_code": "screening",    "stage_status": "pending", "action": "transition" },
    { "stage_code": "screening",    "stage_status": "success", "action": "review",
      "operator": "chenhongwei" },
    { "stage_code": "acceptance",   "stage_status": "pending", "action": "transition" }
  ]
}
```

**判定步骤**：
1. 从后往前找，第 2 条是最后一次 `acceptance + failed + reject` → lastRejectIdx=2 ✅
2. 检查 index=2 之后：
   - index=3: `screening + pending` → 继续
   - index=4: `screening + success` → **已重新初筛！** ❌
3. 结论：已完成返工 → **不显示**

---

### 场景 C：直接验收通过（不显示 ❌）

```json
{
  "id": 1372000,
  "stage_workflow": [
    { "stage_code": "collect",      "stage_status": "success", "action": "transition" },
    { "stage_code": "upload",       "stage_status": "success", "action": "transition" },
    { "stage_code": "post_process", "stage_status": "success", "action": "transition" },
    { "stage_code": "screening",    "stage_status": "success", "action": "review",
      "operator": "chenhongwei" },
    { "stage_code": "acceptance",   "stage_status": "success", "action": "review",
      "operator": "ys-acceptance_a" }
  ]
}
```

**判定步骤**：
1. 从后往前找 `acceptance + failed + reject` → **不存在任何一条** ❌
2. 结论：从未被验收拒绝 → **不显示**
